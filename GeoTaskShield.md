
# 项目：GeoTaskShield

## 项目名称

**GeoTaskShield：面向移动群智感知的隐私保护任务分配仿真系统**


**GeoTaskShield: A Privacy-Preserving Task Allocation Simulator for Mobile Crowdsensing**

---

## 一句话介绍

开发一个 C++/Qt 桌面系统，模拟“平台向移动用户分配感知任务”的过程，在保护用户位置隐私的前提下，实现任务分配、路径代价计算、隐私扰动、算法对比和可视化展示。

体现以下能力：
**C++ 工程能力、算法实现能力、GUI 开发能力、系统设计能力，大语言模型 Agent**，**

---

# 项目场景设计

假设有一个移动群智感知平台，例如智慧交通平台，需要让城市中的用户完成一些任务：

例如：

* 拍摄某路段交通拥堵情况；
* 采集某区域空气质量；
* 上报共享单车分布；
* 监测校园道路人流量；
* 采集某地点噪声数据。

系统中有两类对象：

1. **任务 Task**
   每个任务有位置、奖励、截止时间、所需人数、任务类型。

2. **用户 Worker**
   每个用户有当前位置、移动速度、可接受任务数量、隐私预算、可靠度。

系统目标是：

在用户不暴露真实位置的情况下，尽可能高效地把任务分配给合适的用户。

---

# 核心功能模块

## 1. 任务与用户模拟模块

用 C++ 生成模拟数据。

例如生成：

* 100 个移动用户；
* 50 个感知任务；
* 每个用户有二维坐标；
* 每个任务有二维坐标；
* 每个用户有可用时间、速度、可靠度；
* 每个任务有奖励、截止时间、优先级。

可以先不用真实地图，预留后期使用真实地图，目前直接用二维平面模拟城市区域。

后续可以把场景包装成：

**“广州大学城智慧交通感知任务分配场景”**


---

## 2. 位置隐私保护模块

这个模块是项目亮点。

可以实现 2 到 3 种隐私保护方法：

### 方法一：网格化位置模糊

把真实位置映射到网格区域中。

例如用户真实位置是：

```text
(32.4, 58.7)
```

系统只暴露：

```text
Grid[3][5]
```

这样平台只能知道用户大概在哪个区域，而不知道精确坐标。

---

### 方法二：k-匿名位置保护

让一个用户的位置和附近至少 k-1 个用户混在一起。

例如设置：

```text
k = 5
```

表示系统返回的匿名区域里至少包含 5 个用户，平台不能判断具体是哪一个人。

---

### 方法三：拉普拉斯噪声扰动

给用户坐标加入随机噪声，模拟差分隐私思想。

例如：

```text
真实位置: (40.2, 66.8)
扰动位置: (43.5, 64.1)
```

你可以设置隐私参数 ε：

* ε 越小：隐私更强，但任务分配准确率更低；
* ε 越大：任务分配更准，但隐私保护更弱。


---

## 3. 任务分配算法模块

实现几个算法进行对比。

### 算法一：最近距离贪心算法

每个任务分配给距离最近的用户。

优点：简单、好实现。
缺点：可能造成全局效果不好。

---

### 算法二：综合评分贪心算法

设计一个评分函数：

```text
score = α × 用户可靠度 
      + β × 任务奖励 
      - γ × 移动距离 
      - δ × 隐私损失
```

然后给每个任务选择分数最高的用户。

---

### 算法三：匈牙利算法或最小费用最大流


---

## 4. GUI 可视化模块

用 **Qt + C++** 做桌面 GUI。

界面可以分成几个区域：

### 左侧参数面板

可以设置：

* 用户数量；
* 任务数量；
* 隐私参数 ε；
* k-匿名参数 k；
* 网格大小；
* 任务分配算法；
* 是否启用位置扰动；
* 是否显示真实位置。

### 中间可视化区域

显示二维地图：

* 蓝点：用户；
* 红点：任务；
* 虚线：任务分配关系；
* 半透明区域：匿名区域或网格区域。

### 右侧结果面板

显示实验指标：

* 任务完成率；
* 平均移动距离；
* 平均分配耗时；
* 用户隐私损失；
* 任务收益；
* 算法运行时间。

### 底部日志面板

显示系统运行日志：

```text
[INFO] Generated 100 workers and 50 tasks.
[INFO] Privacy mechanism: k-anonymity, k = 5.
[INFO] Algorithm: Privacy-aware greedy assignment.
[INFO] Completed tasks: 43 / 50.
[INFO] Average moving cost: 12.37.
```

---



代码必须体现：

* 面向对象设计；
* 接口抽象；
* 策略模式；
* 模块解耦；
* 可扩展算法框架。


---

## LLM/Agent 体现

设计一个：

**实验配置 AIAgent**

功能是允许用户输入自然语言让 AIAgent 自动生成实验配置、调用任务分配算法、分析实验结果、生成报告：





